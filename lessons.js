/* ============================================================
   ACG2021 Exam 3 — LESSONS override (optional)
   The comprehensive Learn content already lives in CHAPTERS[].sections
   (data.js), including 2x2 framework grids via the `matrix` field.
   Leaving LESSONS empty makes Learn fall back to those sections, so
   there is a single source of truth. Add LESSONS[chId] = [ ...sections ]
   here only if you want to override a whole chapter's Learn view.
   ============================================================ */

const LESSONS = {};
